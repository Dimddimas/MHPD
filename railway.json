{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "COMPOSER_ALLOW_SUPERUSER=1 composer install --optimize-autoloader --no-scripts --ignore-platform-reqs"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT -t public",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}