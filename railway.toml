[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = "php bin/console doctrine:migrations:migrate --no-interaction --env=prod"
startCommand = "php -S 0.0.0.0:$PORT -t public"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10